"name": "asset-manager",
  "private": true,
  "type": "module",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "build": "npx vite build && npx esbuild server/index.ts --platform=node --bundle --format=esm --outfile=dist/server.js",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.11.5",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "vite": "^5.2.0",
    "esbuild": "^0.20.2"
  }
}
